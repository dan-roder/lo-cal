import { FeaturedImagePipe } from './featured-image.pipe';

describe('FeaturedImagePipe', () => {
  it('create an instance', () => {
    const pipe = new FeaturedImagePipe();
    expect(pipe).toBeTruthy();
  });
});
