import { AllergenRolloverPipe } from './allergen-rollover.pipe';

describe('AllergenRolloverPipe', () => {
  it('create an instance', () => {
    const pipe = new AllergenRolloverPipe();
    expect(pipe).toBeTruthy();
  });
});
