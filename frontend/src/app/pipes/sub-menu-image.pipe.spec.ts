import { SubMenuImagePipe } from './sub-menu-image.pipe';

describe('SubMenuImagePipe', () => {
  it('create an instance', () => {
    const pipe = new SubMenuImagePipe();
    expect(pipe).toBeTruthy();
  });
});
