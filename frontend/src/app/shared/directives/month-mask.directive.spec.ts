import { MonthMaskDirective } from './month-mask.directive';

describe('MonthMaskDirective', () => {
  it('should create an instance', () => {
    const directive = new MonthMaskDirective();
    expect(directive).toBeTruthy();
  });
});
